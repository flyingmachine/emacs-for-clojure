;; markdown is a syntax for writing prose documents in plain text
;; It is very commonly used for README files and documentation
;; https://daringfireball.net/projects/markdown/syntax
(setup (:package markdown-mode)
  (:hook auto-fill-mode))
